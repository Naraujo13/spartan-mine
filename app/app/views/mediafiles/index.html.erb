<%= app_header%>

	<%=app_header_page%>

			<%=list_header @mediafiles, mediafiles_path%>


					<% @mediafiles.each do |mediafile| %>

						<%=list_item mediafile.id, mediafile.references, mediafile.updated_at, edit_mediafile_path(mediafile),"chat",true%>

					<% end %>

			<%=list_footer %>

		<%= app_footer_page @mediafiles, mediafiles_path%>

<%= app_footer%>
<%= app_botton_add new_mediafile_path %>
