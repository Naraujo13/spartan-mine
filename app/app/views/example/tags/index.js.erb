<%=@page_index = params[:tab]%>
$('#collection_<%=params[:tab]%>').html('')
<%
 @example_tags.each do |registro|
  %>
    $('#collection_<%=params[:tab]%>').append(`<%=list_item registro.id,registro.title,registro.updated_at, edit_example_tag_path(registro),'chat'%>`);
  <%
 end
%>
$('#paginator_<%=params[:tab]%>').html('<%=escape_javascript(paginate(@example_tags, :remote => true).to_s) %>');
