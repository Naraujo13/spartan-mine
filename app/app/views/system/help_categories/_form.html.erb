<%= form_for(@system_help_category) do |f| %>
  <% if @system_help_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_help_category.errors.count, "error") %> prohibited this system_help_category from being saved:</h2>

      <ul>
      <% @system_help_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
